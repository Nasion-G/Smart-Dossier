apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: techtrek 
spec:
  selector:
    app: backend
  ports:
  - port: {{ .Values.service.port }}
    targetPort: {{ .Values.service.port }}
  type: ClusterIP